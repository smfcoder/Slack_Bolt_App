# Slack_Bolt_App
This app is used designed to handle the code or run the python applications or hit API's from the Slack interface to a Python backend. We can implement anything in the backend lie connect to Agent or hit the API, etc
